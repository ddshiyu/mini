<!--pages/cardDetail/cardDetail.wxml-->
<view class="user-options">
		<view class="user-cell" wx:for="{{detail}}" wx:key='id'>
			<text class="cell-first">{{item.name}}</text>
			<text>{{item.clocktime}}</text>
		</view>
	</view>